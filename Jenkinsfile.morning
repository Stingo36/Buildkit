pipeline {
    agent {
        label '172.16.218.190'
    }

    environment {
        PROJECT_DIR = '/var/www/html/Buildkit'
        REPO_URL = 'git@github.com:Stingo36/Buildkit.git'
        BRANCH = 'main'
    }

    options {
        skipDefaultCheckout(true)
    }

    stages {
        stage('Clone Repository via SSH') {
            steps {
                script {
                    // Clone the repo into a temp folder inside the workspace
                    sh "git clone -b ${BRANCH} ${REPO_URL} repo_temp"
                }
            }
        }

        stage('Sync Code to Project Directory') {
            steps {
                script {
                    // Rsync code to the Drupal project directory (non-destructive)
                    sh "rsync -av repo_temp/ ${PROJECT_DIR}/"
                }
            }
        }

        stage('Composer Install') {
            steps {
                dir("${env.PROJECT_DIR}") {
                    sh 'composer install'
                }
            }
        }

        stage('Drush Cache Rebuild') {
            steps {
                dir("${env.PROJECT_DIR}") {
                    sh './vendor/bin/drush cr'
                }
            }
        }

        stage('Drush Config Import') {
            steps {
                dir("${env.PROJECT_DIR}") {
                    sh './vendor/bin/drush cim -y'
                    sh './vendor/bin/drush cr'
                }
            }
        }
    }

    post {
        failure {
            echo "❌ Build failed. Check logs for more info."
        }
        success {
            echo "✅ Build completed successfully."
        }
    }
}
